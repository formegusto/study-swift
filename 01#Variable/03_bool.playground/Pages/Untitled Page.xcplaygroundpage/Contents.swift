import UIKit

// Booleans
var isOpen: Bool = true

if isOpen {
    print("is Opened!")
} else {
    print("isn't Opened!")
}

if !isOpen {
    print("isn't Opened!")
} else {
    print("is Opened!")
}
